# WearBlend - Virtual Try-On Application
# Core dependencies (only what's actually imported)

# Web framework
streamlit>=1.40.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# Background removal (with built-in fallback if unavailable)
rembg>=2.0.50

# AI try-on providers (optional features)
openai>=1.0.0
google-genai>=1.0.0

# HTTP + env
requests>=2.31.0
python-dotenv>=1.0.0
